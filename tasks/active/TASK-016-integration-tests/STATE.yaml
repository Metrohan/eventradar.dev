task_id: TASK-016-integration-tests
title: "fix(ci): skip integration tests by default in pytest"
status: "planning"
current_owner: "implementer"
branch: feat/i18n-foundation

completed_steps:
  - "Read pytest.ini — addopts confirmed"
  - "Read tests/integration/test_scrapers_real.py — all 4 tests @pytest.mark.integration"
  - "Read tests/conftest.py — existing fixtures seen"
  - "Determine approach: conftest.py hook (not addopts change)"

current_step:
  description: "Add pytest_collection_modifyitems hook to tests/conftest.py"
  file: "tests/conftest.py"

remaining_steps:
  - "Add hook to conftest.py"
  - "Verify: pytest collects but skips integration tests"
  - "Verify: pytest -m integration still runs live tests"
  - "Verify: coverage threshold still passes"
  - "Commit"
  - "Close GitHub issue #16"

modified_files: []

commands_run:
  - "cat pytest.ini (read)"
  - "grep @pytest.mark.integration tests/integration/test_scrapers_real.py (read)"
  - "cat tests/conftest.py | head -30 (read)"

known_issues:
  - "addopts approach would break 'pytest -m integration' — conftest hook preferred"

assumptions:
  - "conftest.py hook approach is safe and does not affect existing unit/integration test behavior"

review_status: "not_started"
last_updated_at: "2026-08-03"
last_updated_by: "recovery-session"
