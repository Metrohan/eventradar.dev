# PostgreSQL Configuration
POSTGRES_DB=event_scraper_db
POSTGRES_USER=app_user
POSTGRES_PASSWORD=change_this_password
POSTGRES_HOST=db

# Application Secret Key (generate a strong random key)
FLASK_SECRET_KEY=change_this_to_a_random_secret_key

# Database URL (adjust based on above settings)
SQLALCHEMY_DATABASE_URI=postgresql://app_user:change_this_password@db:5432/event_scraper_db

# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_this_password
